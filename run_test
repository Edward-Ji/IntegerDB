#!/usr/bin/env bash

test_dir='./tests'

for in_file in $(find "${test_dir}" -name '*_in'); do
    out_file=${in_file/_in/_out}
    ./ymirdb_cov < "${in_file}" | diff "${out_file}" -
    if [[ $? ]]; then
        echo "passed $(basename $in_file .in)"
    fi
done
